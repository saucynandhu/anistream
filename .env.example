# Base URL for all AniPub API requests (no trailing slash).
# Leave unset in development — Vite proxies `/anipub-api` → `https://anipub.xyz`.
# For static production hosting, set this to your reverse-proxy URL.
# VITE_API_BASE=https://your-proxy.example.com/anipub
